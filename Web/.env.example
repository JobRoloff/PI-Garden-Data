# WebSocket URL for the garden data API (default: ws://localhost:8000/ws)
# VITE_WS_URL=ws://localhost:8000/ws

# When running the API for this app, set CORS_ORIGINS to include the Vite dev origin, e.g.:
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
