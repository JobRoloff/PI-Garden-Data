## Getting Started 

Notes:
- Only needed for dev purposes, the docker compose should handle all this
- Docker doesn't need venv

python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt


